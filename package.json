{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.27.2",
    "react": "^16.14.0",
    "react-dom": "^16.14.0",
    "react-pdf": "^4.2.0",
    "react-router-dom": "^5.3.1",
    "react-scripts": "^2.1.8"
  },
  "proxy": "http://harjitkarmacharya.com/app",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "install-client": "npm install && cd backend && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
